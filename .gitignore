# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Per-run artifacts from the compound-engineering ce:review skill.
# These are debug output, not source. Re-running ce:review creates
# a new timestamped directory each time.
.context/

# Lighthouse / perf JSON traces are large, regeneratable, and have
# historically leaked third-party API keys (see commit 9c48317).
# The accompanying .md reports under docs/perf/ are intentionally
# kept; the raw traces are not.
docs/perf/lighthouse/
docs/perf/lighthouse-stage2/
